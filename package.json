{
  "name": "noxel-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cloud + public sharing shell for Noxel Studio (Supabase-backed).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "gen": "node scripts/gen.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
